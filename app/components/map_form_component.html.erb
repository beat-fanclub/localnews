<%= render MapComponent.new(map: @map_options) do %>
  <% if @new_point %>
    <%= render MarkerComponent.new(nil, nil, @editable, target: "map.markerTemplate") %>
  <% else %>
    <%= render MarkerComponent.new(@point, nil, @editable) %>
  <% end %>
  <%= content %>
<% end %>

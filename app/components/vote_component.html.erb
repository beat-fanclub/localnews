<%= component_controller(class: "voter") do %>
  <% if can? :vote, @voteable %>

    <%= reflex_tag voted?&.up? ? :unvote : :upvote, :button, "➕", {
      class: voted?&.up? ? "active" : ""
    } %>
    <%= @voteable.score %>
    <%= reflex_tag voted?&.down? ? :unvote : :downvote, :button, "➖", {
      class: voted?&.up? ? "active" : ""
    } %>

  <% else %>
    <%= @voteable.score %>
  <% end %>
<% end %>
